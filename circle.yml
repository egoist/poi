machine:
  node:
    version: 6

test:
  override:
    - nvm use 4 && npm test
    - nvm use 5 && npm test
    - nvm use 6 && npm test

deployment:
  staging:
    branch: master
    commands:
      - './node_modules/.bin/nyc report --reporter=text-lcov | ./node_modules/.bin/coveralls'
